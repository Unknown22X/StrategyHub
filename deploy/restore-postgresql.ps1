throw "This legacy database script is retired. Restore RangeBot only through the localhost Backups API, which validates SQLite, creates a safety backup, migrates, reconciles, and keeps Emergency Stop active."
