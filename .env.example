# Keep this file free of real values. Copy to the protected config directory as .env.
# StrategyHub uses a local SQLite database under the selected RANGEBOT_HOME by default.
# No database server or RANGEBOT_DATABASE_URL is required for the Paper demo.
GATE_TESTNET_KEY=
GATE_TESTNET_SECRET=
GATE_LIVE_KEY=
GATE_LIVE_SECRET=
